<!-- One issue per pull request. Link it below, keep the diff small enough to
read in one pass, and tick every box that is true. An unticked box is fine
if you say why in the description. -->

Closes #

## What changes, in one paragraph

## Checklist

- [ ] This pull request addresses one issue, linked above.
- [ ] It is small enough to read in one pass; unrelated changes are split out.
- [ ] Tests are added or updated for the behaviour that changes.
- [ ] The gate is green locally (`npm run gate`, or `npm test` where there is no gate) and its last lines are pasted below.
- [ ] No version edit: `manifest.json`, `package.json` and `versions.json` are untouched.
- [ ] No `CHANGELOG.md` edit; the maintainer writes it at release.
- [ ] No new dependency; or an issue agreed to it and is linked above.
- [ ] Nothing here touches credentials, a network host, a spawned process or a workflow file; or it does, and the description says so.
- [ ] Every commit carries a `Signed-off-by:` line (`git commit -s`, Developer Certificate of Origin 1.1).

## Gate output

```
paste the last lines here
```
